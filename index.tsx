/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// For Expo, we need to use the app name from app.json
// Expo automatically handles the registration, but we need to match the name
AppRegistry.registerComponent('main', () => App);

